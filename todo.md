# recalc a specific postion in the map
scope.countProjectPosition(cluster[0].x, cluster[0].y)

# set a specific postion in the map
angular.element("#m-map-container").animate({
                            scrollTop: top,
                            scrollLeft: left
                        }, 0)